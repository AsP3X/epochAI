# Run tests and lint

From the repo root:

```bash
.venv/bin/ruff check .
.venv/bin/python -m pytest
```

Report command outcomes and test counts before claiming work complete (`definition-of-done.mdc`).
