# Core runtime dependencies (reliability-critical, must always install cleanly).
# Optional/heavier integrations (ccxt, vectorbt, mlflow, river, pandas_ta) live in
# requirements-optional.txt and are imported lazily with graceful fallbacks.
numpy>=1.26,<3
pandas>=2.1,<3
pyarrow>=14
pydantic>=2.5,<3
PyYAML>=6
lightgbm>=4.1
scikit-learn>=1.3
